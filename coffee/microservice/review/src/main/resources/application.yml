spring:
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://localhost:3306/coffee?characterEncoding=UTF-8&serverTimezone=UTC
    username: geekseong_coffee
    password: tjdfkrdnjs~1
    hikari:
      maximum-pool-size: 10
  h2:
    console:
      enabled: true
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
